# Hello Docker!
---
## 1. Run container
* docker run
    ```sh
    docker run -p 80:80 --name nginx-container -t nginx:latest
    ```

## 2. Delete container
* docker rm
    ```sh
    docker rm nginx-container
    ```
